class Solution {
public:
    vector<int> findDisappearedNumbers(vector<int>& nums) {
        // set<int> s;
        // int n = nums.size();
        // for(auto x:nums){
        //     s.insert(x);
        // }
        // nums = vector(s.begin(),s.end());
        sort(nums.begin(),nums.end());
        vector<int> res;
        int temp =1;
        
    }
};
